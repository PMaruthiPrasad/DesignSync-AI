[pytest]
testpaths = tests
# The bundled demo repository is DATA, not code we run. Keep pytest from
# collecting its test_*.py files as part of our own suite.
norecursedirs = app/demo_repository data .venv
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
